service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      allow read: if true; // Everyone can see the website
      allow write: if request.auth != null; // Only logged-in users can edit
    }
  }
}